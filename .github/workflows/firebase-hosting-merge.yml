# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
      - develop
      - release
      - "feature/**"
      - "release/**"

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]

    environment: ${{ ((github.ref == 'refs/heads/main'|| github.event.pull_request.base.ref == 'main') && 'production') || ((github.ref == 'refs/heads/release' || github.event.pull_request.base.ref == 'release') && 'release') || ((github.ref == 'refs/heads/develop' || github.event.pull_request.base.ref == 'develop') && 'development') || 'development' }}
   
    steps:
      - uses: actions/checkout@v2
   
     ## Setup node
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}

      ## Install dependencies
      - name: yarn install
        run: |
          yarn

      - name: Env Setup & Build
        env:
          REACT_APP_API_KEY: ${{secrets.REACT_APP_API_KEY}}
          REACT_APP_CUSTOM_ID: ${{secrets.REACT_APP_CUSTOM_ID}}
        run:  | 
          CI=true && yarn build
      
      # - uses: FirebaseExtended/action-hosting-deploy@v0
      #   with:
      #     repoToken: '${{ secrets.GITHUB_TOKEN }}'
      #     firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_FIR_GITHUB_ACTIONS_FED89 }}'
      #     channelId: live
      #     projectId: fir-github-actions-fed89
      - name: Env
        if: contains(github.ref, 'refs/heads/feature/') || github.ref == 'refs/heads/develop'
        run: echo "Environment develop"
      - name: Env
        if: github.ref == 'refs/heads/release' || github.event.pull_request.base.ref == 'release'
        run: echo "Environment staging"
      - name: Env
        if: github.ref == 'refs/heads/main' || github.event.pull_request.base.ref == 'main'
        run: echo "Environment main"

      ## Deploy Dev
      - name: Deploy Dev
        if: contains(github.ref, 'refs/heads/feature/') || github.ref == 'refs/heads/develop'
        run: |
          yarn firebase deploy --only hosting -P development --token ${{ SECRETS.FIREBASE_TOKEN }} --message "$FIREBASE_MESSAGE"
      
      ## Deploy Staging
      - name: Deploy Staging
        if: contains(github.ref, 'refs/heads/release/') || github.ref == 'refs/heads/release'
        run: |
          yarn firebase deploy --only hosting -P staging --token ${{ SECRETS.FIREBASE_TOKEN }} --message "$FIREBASE_MESSAGE"
      
      ## Deploy Production
      - name: Deploy Production
        if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/release'
        run: |
          yarn firebase deploy --only hosting -P production --token ${{ SECRETS.FIREBASE_TOKEN }} --message "$FIREBASE_MESSAGE"